workload_api {
    socket_path = "/run/spire/sockets/agent.sock"
    trust_domain = "partner.example.com"
}

domains = [
    "oidc.partner.example.com",
    "localhost"
]

acme {
    directory_url = ""
    tos_accepted = false
}

listen_address = "0.0.0.0"
listen_port = 8443

log_level = "INFO"

# Allow insecure scheme for local development
allow_insecure_scheme = true
